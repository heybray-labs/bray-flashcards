DATABASE_URL=postgresql://postgres:postgres@localhost:5441/flashcards
PORT=3102
VITE_PORT=5175

# remember to change this in production
JWT_SECRET=change-me-in-production

# Cover images (local filesystem; relative to repo root). dev.sh sets this if omitted.
# VITE_API_PORT defaults from PORT when using npm run dev.
MEDIA_DIR=data/media
